{
  "private": true,
  "license": "MIT",
  "type": "module",
  "scripts": {
    "dev": "vite dev",
    "build": "vite build",
    "deploy": "touch build/.nojekyll && gh-pages -d build -t true"
  },
  "devDependencies": {
    "@sveltejs/adapter-static": "1.0.5",
    "@sveltejs/kit": "1.2.1",
    "gh-pages": "^5.0.0",
    "svelte": "^3.55.1",
    "vite": "^4.0.4"
  }
}
